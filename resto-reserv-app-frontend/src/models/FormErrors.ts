export type FormErrors = {
  customerName?: string;
  phoneNumber?: string;
  tableId?: string;
  date?: string;
  time?: string;
  partySize?: string;
};

export type NewTableErrors = {
  capacity?: string;
};
